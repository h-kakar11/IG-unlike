[pytest]
testpaths = tests
markers =
    integration: drives a real browser against the local mock Instagram site
filterwarnings =
    error::DeprecationWarning:instagram.*
    error::DeprecationWarning:core.*
